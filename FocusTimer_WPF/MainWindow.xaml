<Window x:Class="FocusTimer_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FocusTimer_WPF"
        mc:Ignorable="d"
        Title="Focus Timer" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="47"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Focus Timer"
                   Grid.Row="0"
                   HorizontalAlignment="Center"
                   FontSize="30"
                   FontWeight="Bold"/>

        <TextBlock x:Name="TimerText"
                   Text="25:00"
                   Grid.Row="1" Grid.Column="1"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="45"
                   FontWeight="Bold"/>

        <TextBlock Text="⏳ Продолжительность:"
                   Grid.Row="3" Grid.Column="0"
                   Margin="75 0 0 0"/>

        <Button x:Name="StartButton"
                Content="Старт"
                Grid.Row="2"
                Width="80"
                Height="35"
                Margin="-100 0 0 0"
                Click="StartButton_Click"/>
        
        <Button x:Name="ResetButton"
                Content="Сброс"
                Grid.Row="2"
                Width="80"
                Height="35"
                Margin="100 0 0 0"
                Click="ResetButton_Click"/>

        <ComboBox x:Name="TimeBox" Grid.Row="2" Grid.Column="0" Grid.RowSpan="2" Margin="210 60 90 0"/>
    </Grid>
</Window>
